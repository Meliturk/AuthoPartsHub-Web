@{
    ViewData["Title"] = "Erişim Reddedildi";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card border-0 shadow-sm rounded-4">
                <div class="card-body p-4">
                    <h4 class="fw-bold mb-2"><i class="bi bi-shield-lock me-2"></i>Erişim Reddedildi</h4>
                    <div class="text-secondary mb-3">Bu sayfaya erişim için yetkiniz yok.</div>
                    <a class="btn btn-accent" asp-controller="Home" asp-action="Index">Ana Sayfa</a>
                </div>
            </div>
        </div>
    </div>
</div>
